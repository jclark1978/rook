{
  "name": "rook-online",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run -w @rook/web dev & npm run -w @rook/server dev",
    "build": "npm run -w @rook/engine build && npm run -w @rook/server build && npm run -w @rook/web build",
    "start": "node apps/server/dist/index.js",
    "test": "npm run -ws test",
    "lint": "npm run -ws lint",
    "typecheck": "npm run -ws typecheck"
  }
}
