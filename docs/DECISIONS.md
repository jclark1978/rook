# Decisions log

Keep short notes when we lock rules/architecture choices.

## 2026-02-08
- Initial rules preset documented in `docs/RULES.md`.
- Repo bootstrapped as TS monorepo: web (React/Vite), server (Express/Socket.IO), engine (pure TS + Vitest).
- Vite dev host allowlist includes `skippy.theclarks.home`.
